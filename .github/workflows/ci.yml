name: CI

# Runs on every PR, and is reusable (called by deploy.yml as its gate before a
# live deploy). No secrets are used or required here.
on:
  pull_request:
  workflow_call:

jobs:
  flutter:
    name: Flutter — analyze + test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: kafi_app
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          # Pin to the project's Flutter version for reproducible builds.
          flutter-version: '3.35.7'
          channel: stable
          cache: true
      - run: flutter pub get
      - run: flutter analyze
      - run: flutter test

  admin:
    name: Admin panel — build (tsc + vite)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: admin-panel
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: admin-panel/package-lock.json
      - run: npm ci
      # `npm run build` is `tsc && vite build` — full type-check + bundle, using
      # mock config (no secrets). That is the real gate here. (There is no ESLint
      # flat config / TS parser wired up, so `npm run lint` is a no-op we skip.)
      - run: npm run build

  functions:
    name: Cloud Functions — build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: functions
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: functions/package-lock.json
      - run: npm ci
      - run: npm run build
