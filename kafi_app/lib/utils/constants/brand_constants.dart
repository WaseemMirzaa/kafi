/// Brand asset paths (bundled under [kafi_app/assets/images/]).
class BrandConstants {
  BrandConstants._();

  static const imagesDir = 'assets/images/';

  /// Full Kafi logo lockup (wordmark + mother/child mark).
  static const kafiLogoAsset = '${imagesDir}kafi-logo.png';
}
